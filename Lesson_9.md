# Занятие 9. Константы

```
// Вынес значение ключевой ставки ЦБ в константу CENTRAL_BANK_KEY_RATE
```

```
// Вынес текст ошибки при выполнении рассчётов в константу CALCULATE_ERROR_DETAILS
```

```
// Заменил невыразительное и некорректное имя для константы const на MAX_REQUEST_TERM (макимальный срок жизни запроса)
```

```
// Вынес заголовки (headers) запроса в константы, например, CONTENT_TYPE
```

```
// Создал константу-рубильник IS_NEW_PARAMS_AVAILABLE для включения/выключения новой логики
```

```
public static final Predicate<User> IS_VIP = (user) -> VIP.equalsIgnoreCase(user.getStatus());
// Вынес условие в константу-предикат, так как оно многократно используется в разных частях программы
```

```
// Заменил магическое число 1000, на константу ITER_NUM, обозначаущую число итераций до сходимости
```

```
// Вынес в константу RUS_MAJORITY_AGE возраст совершеннолетия (в РФ)
```

```
// Заменил название константы ZERO на название EMPTY_SIZE, чтобы оно характеризовало абстрактную сущность
```

```
// Вынес значение 259200 (кол-во секунд 3 днях) в константу TIME_LOG_LIFE (время хранения записи в логах)
```

```
// Вынес параметры подключения к БД в константы (например, DRIVER_CLASS_NAME)
```

```
// Вынес в константу TOLERANCE значение 1e-6 (точность, до которой нужно выполнять высление значения)
```